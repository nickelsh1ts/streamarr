version: '3'
services:
  overseerr:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - 6064:6064
    volumes:
      - .:/app:rw,cached
      - /app/node_modules
      - /app/.next
