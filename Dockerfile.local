FROM node:24-alpine AS base

# Install dependencies only when needed
FROM base AS deps

COPY . /app
WORKDIR /app

RUN corepack enable
RUN yarn set version stable

RUN yarn

FROM deps AS runner

WORKDIR /app

CMD ["yarn", "dev"]
